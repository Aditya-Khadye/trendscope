# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# uv / virtualenv
.venv/
.uv-cache/

# Test/type/lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# Project data (DuckDB lives here, never committed).
# Leading slash anchors to repo root so we don't shadow src/trendscope/data/.
/data/
/digests/
*.duckdb
*.duckdb.wal

# Secrets
.env
.env.local
.env.*.local

# Local config overrides (private — e.g. real holdings in universe.local.yaml)
*.local.yaml

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Build artifacts
build/
dist/
*.egg-info/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
